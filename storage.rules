rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Bloqueia acesso direto do client
    // Upload apenas via signed URLs geradas pelo backend
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
